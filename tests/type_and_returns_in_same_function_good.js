/** If so desired, the documentor may break up desc and
 *  type of return value (like in jsdoc) .*/

/**
 * @function
 * This function returns a String.
 * @return The name of the current user
 * @type String
 */
